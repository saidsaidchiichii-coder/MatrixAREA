# Copy this file to ".env" and fill in your real key.
# The real .env is git-ignored and will NEVER be committed.
#   cp .env.example .env
GEMINI_API_KEY=your_gemini_api_key_here
MATRIX_MODEL=gemini-2.5-flash
